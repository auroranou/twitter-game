<h2>Groups</h2>

<% @groups.each do |g| %>
	<%= g.name %> | 
	<%= link_to 'Join group', '/groups/' + g.id.to_s + '/join_group' %><br>
	<%= link_to 'See scoreboard', group_path(g) %><br>
<% end %>

<h3>New Group</h3>
<%= render 'form' %>